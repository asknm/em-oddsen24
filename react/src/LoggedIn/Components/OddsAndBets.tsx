import { FirebaseMatchWithId } from "../../types/Match"

type OddsAndBetsProps = {
    matchDayId: string,
    match: FirebaseMatchWithId,
    uid: string,
}

export default function OddsAndBets(props: OddsAndBetsProps){


    return <div>

    </div>
}